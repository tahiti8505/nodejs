// 반올림 함수
console.log(Math.round(3.2));


function sum(first, second){ // 매개변수 -> parameter
  console.log(first+second);
}

function sum(first, second){
  return first+second;
}

sum(2,4); // 입력값 -> 인자 argument
console.log(sum(2,4));
